#include <iostream>
using namespace std;

int main() {
    
// //     // Declaration
// // //   string s = "nikhil";
// // // //   string s("nikhil thakur");
// // // //   s = "nikhil thakur";
   
// // // //   string s;
// // // //   getline(cin, s);
// // // int len =s.length();

// // // // for(int i=0;i<6;i++){
// // // //      cout <<s[i]<< " ";
// // // // }   

// // // string s2 = "thakur";
// // //  cout <<s+ " " <<s2;/

// string s = "nikhil thakur";
// // string temp = s.substr(0,9);

// // cout<<temp;
// // cout<<endl;
// // temp.push_back('a');
// // temp.pop_back(); temp.pop_back(); temp.pop_back(); temp.pop_back();temp.pop_back();temp.pop_back();
// // cout<<temp;
     
//       for(auto& x : s){
//           cout<<x << " ";
//       }
    //   string s = "kanak";
    //   int len = s.length();
      
    //   int i=0,j=len-1;
    //   bool flag = true;
      
    //   while(i<=j){
    //       if(s[i] != s[j]){
    //           flag = false;
    //           break;
    //       }
    //       i++;
    //       j--;
    //   }
    //   if(flag){
    //       cout<< "yes";
    //   }
    //   else {
    //       cout<< "no" <<endl;
    //   }
    
    // int n =3;
    // int arr[n][n];
    
    // for(int i=0;i<n;i++){
    //     for(int j=0;j<n;j++){
    //         cin>>arr[i][j];
    //     }
    // }
    // cout <<endl;
    
    // for(int i=0;i<n;i++){
    //     for(int j=0;j<n;j++){
    //         cout<<arr[j][i]<< " ";
    //     }
    //     cout << endl;
    // }
    
    int n =3;
    int arr[n][n];
    
    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++){
            cin>>arr[i][j];
        }
    }
    cout <<endl;
    
    int freq[1000];
    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++){
            freq[arr[i][j]]++;
        }
    }
    int target = 2;
    cout<<freq [target];
    return 0;
}
